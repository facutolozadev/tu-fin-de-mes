import React from 'react'
import { View } from 'react-native'
import StyledText from '../components/StyledText'

type Props = {}

function WalletsView({}: Props) {
  return (
    <View>
        <StyledText>Wallets</StyledText>
    </View>
  )
}

export default WalletsView